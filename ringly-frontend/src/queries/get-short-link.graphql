query getLinkByShortLink($shortLink: String) {
  link(where: { shortLink: { _eq: $shortLink } }) {
    id
    attributes
    destinationLink
    domain_id
    expireAt
    kind
    shortLink
    variablePassing
    cta_id
  }
}
